const clientConfig = {
    // IP-адрес и порт вашего Java сервера.
    // Если клиент и сервер на одной машине, можно использовать 'localhost' или '127.0.0.1'.
    // Если сервер на другом ПК в той же сети, укажите его IP-адрес.
    // Если вы будете разворачивать сервер где-то еще, укажите соответствующий адрес.
    serverUrl: 'http://localhost:8080/api', // Убедитесь, что порт совпадает с серверным
    defaultRoute: '#/login' // Маршрут по умолчанию
};